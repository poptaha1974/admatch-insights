VITE_API_URL=http://localhost:8000

# Optional: override the default Karseell campaign ID
# VITE_META_CAMPAIGN_ID=120252073443580166

# Meta Marketing API — server-side secrets (never expose to client)
# Set these as Cloudflare Worker secrets: wrangler secret put META_ACCESS_TOKEN
META_ACCESS_TOKEN=
META_ACCOUNT_ID=
